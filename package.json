{
  "private": true,
  "workspaces": [
    "./web",
    "./jyutping-helpers"
  ],
  "devDependencies": {
    "lint-staged": "12.3.2",
    "only-allow": "1.0.0",
    "syncpack": "6.2.0"
  },
  "scripts": {
    "preinstall": "only-allow npm",
    "syncpack": "syncpack list-mismatches",
    "synpack:fix": "syncpack fix-mismatches"
  }
}
