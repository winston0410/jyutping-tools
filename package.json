{
  "private": true,
  "workspaces": [
    "./web",
    "./jyutping-helpers"
  ],
  "devDependencies": {
    "lint-staged": "12.3.2",
    "only-allow": "1.0.0",
    "syncpack": "6.2.0"
  },
  "scripts": {
    "build:vercel": "cd ./jyutping-helpers && npm run build && cd .. && cd ./web && npm run build",
    "preinstall": "only-allow yarn",
    "syncpack": "syncpack list-mismatches",
    "syncpack:fix": "syncpack fix-mismatches"
  }
}
